This application detects if the given image url contains any faces, if yes, then it draws rectangle around the face.
This application is built in react.

Steps to run the application on local:
-- clone the repo
-- run "npm install"
-- run "npm start"
